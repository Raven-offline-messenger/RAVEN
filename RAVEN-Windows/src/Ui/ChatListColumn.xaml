<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="RAVEN.Windows.Ui.ChatListColumn"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:RAVEN.Windows.Ui">

    <!--
      Middle pane on the Messages tab. Mirrors `ChatListColumn.swift`
      (RAVEN-MacApp). Header with title + compose icon, capsule search
      bar, then the inbox list. Selecting a row sets the shared
      `ShellRouter.SelectConversation`, which the right pane (RightPane
      → ChatThreadView) listens to.
    -->
    <Grid Background="{StaticResource RavenBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="20,18,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Messages"
                       FontSize="22" FontWeight="ExtraBold"
                       Foreground="{StaticResource RavenText}"/>
            <Button Grid.Column="1" Width="36" Height="36" Padding="0"
                    Background="Transparent" BorderThickness="0">
                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE70F;" FontSize="14"
                          Foreground="{StaticResource RavenText}"/>
            </Button>
        </Grid>

        <Border Grid.Row="1" Margin="16,4,16,12" Padding="14,10"
                Background="{StaticResource RavenBgCard}"
                CornerRadius="999">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE721;" FontSize="13"
                          Foreground="{StaticResource RavenText3}"
                          VerticalAlignment="Center"/>
                <TextBox PlaceholderText="Search Direct Messages" BorderThickness="0"
                         Background="Transparent" Foreground="{StaticResource RavenText}"
                         FontSize="13" MinWidth="220"
                         VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ListView Grid.Row="2" x:Name="ConversationList"
                  SelectionMode="Single" Padding="6,0,6,12"
                  Background="Transparent"
                  SelectionChanged="OnConversationSelected">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,2"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ConversationVm">
                    <Border Padding="16,10" CornerRadius="14" Background="Transparent">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="44"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22"
                                    Background="{StaticResource RavenLogoGradient}">
                                <TextBlock Text="{x:Bind Initials}"
                                           FontSize="15" FontWeight="Bold"
                                           Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center" Spacing="2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{x:Bind Name}"
                                               FontSize="15" FontWeight="Bold"
                                               Foreground="{StaticResource RavenText}"
                                               TextTrimming="CharacterEllipsis" MaxLines="1"/>
                                    <Border Grid.Column="1" Padding="7,2" CornerRadius="999"
                                            Background="{StaticResource RavenBrandGradient}"
                                            Visibility="{x:Bind UnreadVisibility}">
                                        <TextBlock Text="{x:Bind UnreadText}"
                                                   FontSize="11" FontWeight="Bold"
                                                   Foreground="White"/>
                                    </Border>
                                </Grid>
                                <TextBlock Text="{x:Bind Preview}"
                                           FontSize="13"
                                           Foreground="{StaticResource RavenText3}"
                                           TextTrimming="CharacterEllipsis" MaxLines="1"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
